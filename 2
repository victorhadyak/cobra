package main

import (
    "fmt"
    "github.com/spf13/cobra"
    "os"
    "os/exec"
)

func main() {
    var rootCmd = &cobra.Command{
        Use:   "ssm-cli",
        Short: "SSM CLI to connect to EC2 instances across regions",
    }

    var connectCmd = &cobra.Command{
        Use:   "connect [instance-id]",
        Short: "Connect to an EC2 instance",
        Args:  cobra.MinimumNArgs(1),
        Run:   connectInstance,
    }

    var generateCmd = &cobra.Command{
        Use:   "generate",
        Short: "Generate a new session",
        Run:   generateSession,
    }

    rootCmd.AddCommand(connectCmd, generateCmd)
    rootCmd.Execute()
}

func connectInstance(cmd *cobra.Command, args []string) {
    instanceID := args[0]

    // Generate session before connecting
    _, err := generateTemporaryCredentials()
    if err != nil {
        fmt.Println("Error generating temporary credentials:", err)
        return
    }

    // ... rest of your connectInstance function ...
}

func generateSession(cmd *cobra.Command, args []string) {
    // Generate session only
    _, err := generateTemporaryCredentials()
    if err != nil {
        fmt.Println("Error generating temporary credentials:", err)
        return
    }
    fmt.Println("Session generated successfully.")
}

func generateTemporaryCredentials() (string, error) {
    // ... your existing implementation ...
}
